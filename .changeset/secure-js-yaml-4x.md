---
"@openapi-to/core": patch
---

Update the YAML parser to the maintained secure js-yaml 4.x baseline, preserving OpenAPI YAML anchors and merges while applying the upstream prototype-pollution and parser-complexity protections.
