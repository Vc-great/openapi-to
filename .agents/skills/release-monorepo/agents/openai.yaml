interface:
  display_name: "Release Monorepo"
  short_description: "Prepare Version PRs and controlled package publication"
  default_prompt: "Use $release-monorepo to prepare the two-phase release and trigger publication only with exact SHA, version, channel, and explicit authority."
