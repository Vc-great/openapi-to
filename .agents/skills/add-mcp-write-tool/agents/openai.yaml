interface:
  display_name: "Add MCP Write Tool"
  short_description: "Safely extend controlled MCP generation writes"
  default_prompt: "Use $add-mcp-write-tool to extend the existing Prepare/Apply protocol without adding a direct write shortcut."
